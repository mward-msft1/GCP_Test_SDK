Flask==3.1.3
python-dotenv==1.2.2
requests==2.33.0
azure-identity==1.17.1

# Optional: only required once you enable the Agent Framework middleware.
# agent-framework==0.0.x
# agent-framework-purview==0.0.x
